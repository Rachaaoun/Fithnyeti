{% extends 'baseadmin.html.twig' %}

{% block title %}Complaint type{% endblock %}

{% block body %}
    <h1>Complaint type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ complainttype.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ complainttype.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('complainttype_index') }}">back to list</a>

    <a href="{{ path('complainttype_edit', {'id': complainttype.id}) }}">edit</a>

    {{ include('complainttype/_delete_form.html.twig') }}
{% endblock %}
